<Window x:Class="tema2MAP.PlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:tema2MAP"
        mc:Ignorable="d"
        Title="PlayWindow" Height="450" Width="800">

    <Window.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#c3fcae"/>
            <Setter Property="BorderBrush" Value="#9ac78b"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="10" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="#f7faf0">
        <Button Content="New Game" Foreground="#2d3b28" FontSize="14" HorizontalAlignment="Left"
                Height="60" Margin="65,99,0,0" VerticalAlignment="Top" Width="200" Style="{StaticResource RoundedButtonStyle}"
                Click="NewGame_Click"/>
        <Button Content="Open Game" Foreground="#2d3b28" FontSize="14" HorizontalAlignment="Left" 
                Height="65" Margin="290,194,0,0" VerticalAlignment="Top" Width="200" Style="{StaticResource RoundedButtonStyle}"
                Click="NewGame_Click"/>
        <Button Content="Statistics" Foreground="#2d3b28" FontSize="14" HorizontalAlignment="Left" Height="70" Margin="510,294,0,0" VerticalAlignment="Top" Width="200" Style="{StaticResource RoundedButtonStyle}"/>

    </Grid>
</Window>
